ffmpeg
fastapi
uvicorn
fastmcp
sounddevice
scipy
tensorflow
microwakeword
chatterbox-tts

# chatterbox-tts's resemble-perth dependency still imports the legacy
# pkg_resources API (from setuptools). setuptools>=81 dropped pkg_resources,
# which silently breaks perth.PerthImplicitWatermarker (swallowed
# ImportError -> becomes None -> TypeError when chatterbox instantiates it).
# Pin setuptools below that to keep pkg_resources available:
setuptools<81

# System library required by the sounddevice Python package (PortAudio bindings).
# Not installed automatically — install via your OS package manager:
#   macOS:  brew install portaudio
#   Debian/Ubuntu: apt-get install libportaudio2 portaudio19-dev
portaudio